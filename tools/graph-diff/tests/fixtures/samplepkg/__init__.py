"""Neutral fixture package for graph-diff extraction tests.

Deliberately repo-agnostic: no reference to the host project, its packages, or
any country. The intra-package import edges are fixed and known so the extract
tests can assert an exact node/edge set.
"""
