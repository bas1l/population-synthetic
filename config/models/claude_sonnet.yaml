id: "claude_sonnet"
label: "Claude Sonnet"
model_config:
  provider: "claude"
  model: "sonnet"
  generation_config:
    temperature: null
    top_p: null
    top_k: null
    max_output_tokens: null
parameters:
  parallel:
    workers: 4
