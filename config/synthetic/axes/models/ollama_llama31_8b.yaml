id: "ollama_llama31_8b"
label: "Ollama Llama3.1 8B"
model_config:
  provider: "ollama"
  model: "llama3.1:8b-instruct-q4_K_M"
  base_url: "http://192.168.0.19:11434"
  generation_config:
    temperature: 0.7
    top_p: null
    top_k: null
    max_output_tokens: 2048
parameters:
  parallel:
    # VRAM-max knee per docs/development/ollama-parallelism-poc/REPORT.md (peak 2.15x).
    # Only effective if the server's OLLAMA_NUM_PARALLEL matches; else requests queue.
    workers: 6
