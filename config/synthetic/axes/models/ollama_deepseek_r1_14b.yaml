id: "ollama_deepseek_r1_14b"
label: "Ollama DeepSeek-R1 14B"
model_config:
  provider: "ollama"
  model: "deepseek-r1:14b"
  base_url: "http://192.168.0.19:11434"
  generation_config:
    temperature: 0.7
    top_p: null
    top_k: null
    max_output_tokens: 2048
parameters:
  parallel:
    # VRAM-max knee per docs/development/ollama-parallelism-poc/REPORT.md (peak 1.60x).
    # Only effective if the server's OLLAMA_NUM_PARALLEL matches; else requests queue.
    workers: 2
