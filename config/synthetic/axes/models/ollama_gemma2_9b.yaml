id: "ollama_gemma2_9b"
label: "Ollama Gemma2 9B (discarded for now)"
model_config:
  provider: "ollama"
  model: "gemma2:9b"
  base_url: "http://192.168.0.19:11434"
  generation_config:
    temperature: 0.7
    top_p: null
    top_k: null
    max_output_tokens: 2048
parameters:
  parallel:
    # Single slot only per docs/development/ollama-parallelism-poc/REPORT.md
    # (head_dim=256 => large KV; only 1 slot fits VRAM). No parallelism to gain.
    workers: 1
