"""population_synth.analysis.mapping — raw population data -> canonical schema.

Transforms raw population data (national-statistics reference records *or* LLM
pipeline identities) into the canonical comparable schema consumed by the
comparison process.
"""
